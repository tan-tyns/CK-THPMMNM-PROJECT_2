{
  "name": "render-fullstack-app",
  "version": "1.0.0",
  "description": "Fullstack app for CI/CD demo on Render",
  "main": "server/index.js",
  "scripts": {
    "build": "cd client && npm install && npm run build && cd ../server && npm install",
    "start": "node server/index.js"
  }
}